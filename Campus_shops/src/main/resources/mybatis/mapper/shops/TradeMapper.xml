<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.TradeMapper">

    <!-- 插入销售记录 -->
    <insert id="insertRecord" parameterType="com.entity.Traderecord">insert into traderecord (id,fromid,toid,money,tradetime) value (#{id},#{fromid},#{toid},#{money},#{tradetime})</insert>


</mapper>